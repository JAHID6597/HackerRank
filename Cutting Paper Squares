#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long n, m;
    cin>>n>>m;
    long long mx, mn;
    mx = max(n,m);
    mn = min(n,m);
    cout<<mx-1+(mn-1)*mx<<endl;
    return 0;
}
